export interface Project {
  title: string;
  src: string;
  color: string;
  url: string;
  role: string;
  github: string;
}

export interface ProjectModal {
  active: boolean;
  index: number;
}

export interface isMobileModal {
  active: boolean;
  index: number;
}
